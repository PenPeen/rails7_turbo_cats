<%# 作成したCatを一覧に追加 %>
<%= turbo_stream.prepend "cats-list", @cat %>

<%# 作成フォームを空にする %>
<%#= turbo_stream.update "new_cat", "" %>

<%# Flashメッセージの表示 %>
<%= turbo_stream.append "flashes", partial: "application/flash" %>
